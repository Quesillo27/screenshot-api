{
  "name": "screenshot-api",
  "version": "1.0.0",
  "description": "API REST para tomar screenshots de URLs usando Playwright",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --testTimeout=10000"
  },
  "keywords": ["screenshot", "playwright", "api", "puppeteer"],
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.3",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
